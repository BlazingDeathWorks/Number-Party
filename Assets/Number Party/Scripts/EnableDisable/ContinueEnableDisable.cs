using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace NumberParty.EnableDisable
{
    public class ContinueEnableDisable : MonoBehaviour
    {
        /*[SerializeField] ActionChannel enableChannel = null;
        [SerializeField] ActionChannel disableChannel = null;

        private void Awake()
        {
            enableChannel?.AddAction(ContinueEnable);
            disableChannel?.AddAction(ContinueDisable);
        }

        private void ContinueEnable()
        {
            continueButton.SetActive(true);
        }

        private void ContinueDisable()
        {
            gameObject.SetActive(true);
            continueButton.SetActive(false);
        }*/
    }
}
